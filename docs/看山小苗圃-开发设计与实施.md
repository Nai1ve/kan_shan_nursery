# 看山小苗圃 开发设计与实施

本文档把 `docs/知见Agent-产品与架构设计.md`（产品/架构总纲）、`docs/看山小苗圃-算法与模块详细设计.md`（算法和服务边界）、`docs/开发路线图.md`（版本与里程碑）三份文档落到具体的实施切片，明确每个版本里：

- 各服务的负责 / 不负责
- 文件级别的改动范围
- harness 约定（契约和实现分离的边界）
- 测试和验收口径
- 需要同步更新的文档

> 文档同步约定：每次合并到 main 的 commit，必须同步：本文件对应版本章节、`docs/开发路线图.md` 的版本状态、被改服务的 `README.md`、必要时的 `docs/P0-mock-闭环-测试checklist.md` 或新版本的 checklist。

---

## 1. 现状盘点（v0.6 完成后）

### 1.1 已实现服务

| 服务 | 端口 | 已实现 | 备注 |
| --- | --- | --- | --- |
| api-gateway | 8000 | 统一 envelope、`request_id` 透传、8 下游 ready 控制、5 条 P0 链路 50+ 路由、proxy 事件埋点 | 业务无变 |
| profile-service | 8010 | 演示画像、全局/兴趣 Memory、Memory 注入摘要、update-requests apply/reject、shared logger | mock |
| content-service | 8020 | bootstrap、按分类列卡片、单卡 / 单 source 全文、刷新（轮换+计数）、卡片 summarize、shared logger | mock，未走 llm-service |
| seed-service | 8030 | from-card 幂等、四格材料 CRUD、疑问线程、Agent 补充、合并、成熟度自动重算、shared logger | mock，未走 llm-service |
| sprout-service | 8040 | 主动触发 run、同 interest 同日缓存、opportunities 列表、补料/换角度/不写/开始写作、shared logger | mock，未走 llm-service |
| writing-service | 8050 | 8 步状态机、memoryOverride session 隔离、mock 发布、feedbackHandoff、shared logger | mock，未走 llm-service |
| feedback-service | 8060 | 文章列表/详情、评论摘要、second-seed 输出 payload、memory-update-request 仅产 pending、shared logger | mock，未走 llm-service |
| zhihu-adapter | 8070 | **三类 Client 拆开** (Community/OAuth/DataPlatform) + Redis 缓存 + quota + 错误归一化 + 圈子白名单 + OAuth authorize/callback + 字段归一化 + 全链路日志埋点 | live 模式可联调 |
| llm-service | 8080 | 多 provider 注册表（mock+zhihu_direct+openai_compat stub）、per-task 路由、multi_persona 圆桌审稿、cache、validators、trace、shared logger | 框架就绪，下游待 v0.7+ 接入 |

### 1.2 共享基础设施

| 包 | 内容 |
| --- | --- |
| `packages/shared-python/kanshan_shared` | `load_config()` 三层加载（env > yaml > 默认）、`configure_logging()` jsonl + 控制台双输出、stdlib-only YAML parser |
| `services/config.yaml` (gitignored) | 单一凭证源；模板见 `services/config.example.yaml` |
| `output/logs/<service>-<date>.jsonl` (gitignored) | 各服务结构化日志统一落点 |
| `output/llm-trace/<date>.jsonl` (gitignored) | llm-service 每次 task 调用的 trace |

### 1.3 测试基线

`python3 -m unittest discover -s <svc>/tests` 累计 **80 ok / 0 fail**：
api-gateway 5 · content 9 · feedback 7 · llm 10 · profile 6 · seed 5 · sprout 9 · writing 7 · zhihu 13 · shared-python 9。

### 1.4 工具

- 一键启动：`scripts/run_all_services.sh`
- v0.4 验收清单：`docs/P0-mock-闭环-测试checklist.md`

---

## 2. Harness Engineering 总则

每个版本的实施都遵循以下约定，目的是让契约和实现分离、让"哪个服务用哪个能力"成为配置而非硬编码。

1. **接口契约稳定**：业务服务对 llm-service / zhihu-adapter 只感知 task type 和 endpoint，不感知 provider、prompt、模型 ID。
2. **配置驱动**：provider 注册、task 路由、persona 编排都放配置文件（`config/*.yaml` 或环境变量），代码只做注册表查询。
3. **降级显式**：失败必须显式 `fallback=true` 或 `error.code=...`，不能静默返回看似正常的数据。
4. **trace 必落盘**：每次 LLM 调用 / 知乎调用产 jsonl 一行（task / provider / persona / cache_hit / fallback / latency / token_estimate），方便后期评测。
5. **测试与边界绑定**：每个新版本的"不负责"清单必须有用例反向验证（例如 feedback-service 不能直接写 Memory，要有用例确认调用后 GET memory 不变）。
6. **文档同步**：本文件对应版本章节 + 服务 README + 路线图状态必须和代码同 commit。

---

## 3. 多 LLM 视角 Agent 框架（v0.5 落地）

### 3.1 设计目标

让"哪个 Agent 用哪个 LLM 视角"成为 llm-service 内部的路由问题，业务服务永远只调 `POST /llm/tasks/{task_type}`。

为什么要做：

- 不同任务对模型偏好不同（摘要 vs 反方 vs 草稿）。
- 圆桌审稿天然多视角，单 provider 单 prompt 容易同质化。
- 真实接入外部 provider 时，希望 0 改动业务代码。

### 3.2 架构

```text
business service
        │  POST /llm/tasks/{task_type}
        ▼
    llm-service
    ├─ validators            (request / output schema)
    ├─ cache                 (memory or redis)
    ├─ registry              (load config/providers.yaml)
    ├─ router                (task_type → provider_id 或 multi_persona 列表)
    ├─ providers/
    │    ├─ base.py          (Provider Protocol)
    │    ├─ mock.py          (现 mock_provider 升级)
    │    ├─ zhihu_direct.py  (现 zhihu_provider 升级)
    │    └─ openai_compat.py (stub，等密钥)
    ├─ prompts/v1/
    │    ├─ <task>.md        (主 prompt)
    │    └─ personas/<name>.md (persona prompt)
    └─ trace                 (output/llm-trace/*.jsonl)
```

### 3.3 Provider 抽象

```python
class Provider(Protocol):
    name: str  # "mock" / "zhihu_direct" / "openai_compat"

    def run(
        self,
        task_type: str,
        input_data: dict,
        prompt: str,
        persona: str | None = None,
    ) -> dict:
        """Returns parsed structured output. Raises ProviderError on failure."""
```

所有 provider 共享同一接口；router 拿 provider 后注入对应 prompt（含 persona 段）。

### 3.4 Task × Provider 映射表（v0.5 起作用）

| task_type | 默认 provider | fallback | 模式 | 备注 |
| --- | --- | --- | --- | --- |
| summarize-content | zhihu_direct | mock | single | 高频，cache 优先 |
| extract-controversies | zhihu_direct | mock | single | 高频 |
| generate-writing-angles | zhihu_direct | mock | single | |
| answer-seed-question | zhihu_direct | mock | single | v0.7 由 seed-service 调真实 |
| supplement-material | zhihu_direct | mock | single | v0.7 由 seed-service 调真实 |
| sprout-opportunities | zhihu_direct | mock | single | v0.8 由 sprout-service 调真实 |
| argument-blueprint | zhihu_direct | mock | single | v0.8 由 writing-service 调 |
| draft | zhihu_direct | mock | single | v0.8 |
| roundtable-review | (multi_persona) | mock | multi_persona | v0.5 框架，v0.8 真实落地 |
| feedback-summary | zhihu_direct | mock | single | v0.9 |

> v0.5 仅 mock + zhihu_direct 真实可用。openai_compat 提供 stub 实现，启用需要 `OPENAI_COMPAT_BASE_URL` / `OPENAI_COMPAT_API_KEY`，否则 registry 不注册。

### 3.5 多 Persona（roundtable-review）

```yaml
roundtable-review:
  mode: multi_persona
  personas:
    - id: logic_reviewer
      provider: zhihu_direct
      persona_prompt: prompts/v1/personas/logic_reviewer.md
    - id: human_editor
      provider: zhihu_direct
      persona_prompt: prompts/v1/personas/human_editor.md
    - id: opponent_reader
      provider: zhihu_direct
      persona_prompt: prompts/v1/personas/opponent_reader.md
    - id: community_editor
      provider: zhihu_direct
      persona_prompt: prompts/v1/personas/community_editor.md
```

router 在 multi_persona 模式：

1. 对每个 persona 单独调 provider；并发由 `concurrent.futures.ThreadPoolExecutor` 串起。
2. 每条结果用 `roundtable-review` schema 校验。
3. 聚合到 `output.reviews[]`，每条带 `persona`、`provider` 标注。
4. 顶部按 `severity ∈ {high, medium, low}` 排序。
5. 任一 persona 失败：mock 该 persona 并标 `fallback=true`，不让全任务失败。

### 3.6 trace 输出

每次 `LlmService.run_task` 完成（无论命中缓存与否）写一行：

```json
{"ts":"2026-05-11T...","taskType":"roundtable-review","provider":"multi_persona",
 "persona":null,"cacheHit":false,"fallback":false,"latencyMs":182,
 "promptVersion":"v1","schemaVersion":"v1","inputHash":"abc...","mode":"multi_persona",
 "subCalls":[
   {"persona":"logic_reviewer","provider":"zhihu_direct","fallback":false,"latencyMs":120},
   {"persona":"human_editor","provider":"zhihu_direct","fallback":true,"latencyMs":20}
 ]}
```

文件按日期分片：`output/llm-trace/2026-05-11.jsonl`，目录由服务自动创建。

### 3.7 缓存键

保持现有：`llm:{task_type}:{input_hash}:{prompt_version}:{schema_version}`。
multi_persona 任务整体作为一条缓存（命中后跳过所有 persona），子 persona 不单独缓存——这避免缓存爆炸，也保证 reviewers 一致性。

### 3.8 失败语义

- 主 provider 抛 `ProviderError`：路由按 fallback 配置降级，response 设 `fallback=true`。
- multi_persona 内部任一 persona 抛错：仅该 persona 走 mock 替补；整体不算 fallback；trace 单独记。
- mock 也失败（schema 校验都过不了）：返回 HTTP 502，error.code=`LLM_OUTPUT_INVALID`。

---

## 4. v0.5 实施切片

### 4.1 文件改动范围

```text
services/llm-service/
├── app/
│   ├── providers/                  ← 新增
│   │   ├── __init__.py
│   │   ├── base.py                 ← Provider Protocol + ProviderError
│   │   ├── mock.py                 ← 从 mock_provider.py 迁入（保留 run_mock_task）
│   │   ├── zhihu_direct.py         ← 从 zhihu_provider.py 迁入
│   │   └── openai_compat.py        ← stub，未配 env 不注册
│   ├── registry.py                 ← 新增：加载 providers.yaml + persona 配置
│   ├── router.py                   ← 新增：task_type → provider 或 multi_persona
│   ├── trace.py                    ← 新增：jsonl 落盘
│   ├── service.py                  ← 改：调用 router 而不是 zhihu_provider
│   ├── prompts.py                  ← 保留，新增 load_persona_prompt
│   ├── validators.py               ← 不动
│   ├── cache.py                    ← 不动
│   ├── settings.py                 ← 加 trace_dir / openai_compat_* 字段
│   ├── main.py                     ← 不动
│   ├── mock_provider.py            ← 删除（被 providers/mock.py 取代）
│   └── zhihu_provider.py           ← 删除（被 providers/zhihu_direct.py 取代）
├── config/
│   └── providers.yaml              ← 新增（自带，不需要外部依赖；用 Python stdlib 解析）
├── prompts/v1/
│   └── personas/                   ← 新增
│       ├── logic_reviewer.md
│       ├── human_editor.md
│       ├── opponent_reader.md
│       └── community_editor.md
├── tests/
│   └── test_core.py                ← 扩充：路由/persona/trace/fallback
└── README.md                       ← 同步
```

### 4.2 文档同步

- 本文件 §3、§4（即时）。
- `docs/开发路线图.md` v0.5 状态由 🟡 改为 ✅（v0.5 完成后）。
- `services/llm-service/README.md` 增加多 provider / persona 说明。

### 4.3 不在 v0.5 范围内

- 不接入 OpenAI 兼容外部 provider（保留 stub，env 未配则不注册）。
- 不修改 content / sprout / writing / feedback 服务的调用方式（mock 文本保持原样，留到 v0.7-v0.9 各自版本切换）。
- 不修改前端。

### 4.4 测试切片

- 既有 4 个测试不破坏。
- 新增：
  - `test_router_routes_each_task_to_configured_provider`
  - `test_zhihu_provider_failure_falls_back_to_mock_with_trace`（既有改造）
  - `test_roundtable_review_multi_persona_aggregates_four_reviewers`
  - `test_persona_failure_only_falls_back_that_persona`
  - `test_trace_file_contains_required_fields`
  - `test_openai_compat_provider_not_registered_when_env_missing`

期望 llm-service 测试由 4 → 10 ok。

### 4.5 验收（v0.5）

- 既有所有 task 在 mock 模式仍通过 schema 校验。
- `LLM_PROVIDER_MODE=zhihu` 时主 provider 走真实 zhihu_direct（联调时验证）。
- roundtable-review 在 mock 路由下返回 4 条 reviewer，每条带 `persona` 和 `provider`。
- 同输入二次调用命中 cache。
- `output/llm-trace/<日期>.jsonl` 有结构化记录。
- 仓库全量测试不退化（v0.4 基线 59 ok 全部仍通过）。

---

## 5. v0.6 实施切片（已完成）

### 5.1 服务边界（v0.6）

- `packages/shared-python`：从 README 占位升级为可用包，承载跨服务共享的 config loader + 结构化日志。
- zhihu-adapter：三类 Client 拆分、错误码归一化、OAuth 完整流程、圈子白名单、全链路日志埋点。
- api-gateway：proxy 事件埋点 + shared logger 接入。
- 其他 7 个服务（content / feedback / llm / profile / seed / sprout / writing）：main.py 启动时 `configure_logging`，统一日志落盘，业务逻辑不变。
- frontend：登录页"关联知乎账号"按钮改为跳转到 `zhihu-adapter` 的 `/zhihu/oauth/authorize?redirect=true`。

### 5.2 改动范围（v0.6）

```text
packages/shared-python/
├── kanshan_shared/
│   ├── __init__.py                      ← 新增：load_config / configure_logging / get_logger 导出
│   ├── _bootstrap.py                    ← sys.path 引导工具
│   ├── _yaml.py                         ← 极简 YAML parser（stdlib-only）
│   ├── config.py                        ← KanshanConfig + ZhihuConfig dataclasses + 三层加载
│   └── logger.py                        ← JsonLinesHandler + ConsoleFormatter + configure_logging
├── tests/test_core.py                   ← YAML / config / logging 共 9 个用例
└── pyproject.toml                       ← 新增

services/config.example.yaml             ← 新增凭证模板（services/config.yaml 已被 .gitignore）
.gitignore                                ← 新增 services/config.yaml 规则

services/zhihu-adapter/
├── app/
│   ├── errors.py                        ← 新增：ZhihuApiError 家族 + from_community/oauth/data_platform
│   ├── live_client.py                   ← 重写：拆 CommunityClient / OAuthClient / DataPlatformClient + HTTP 错误归一化
│   ├── main.py                          ← 加 OAuth authorize / callback 路由 + shared logger + 统一错误响应
│   ├── service.py                       ← 用三 client 替换旧单 client；加白名单守卫、埋点、quota 从 config 读
│   └── settings.py                      ← 薄 facade，代理 kanshan_shared.load_config
└── tests/test_core.py                   ← 从 7 扩展到 13 个用例

services/api-gateway/app/main.py         ← 加 shared logger + proxy 事件埋点
services/{content,feedback,llm,profile,seed,sprout,writing}-service/app/main.py
                                          ← 每个 main 在 FastAPI(...) 后注入 configure_logging + logger

frontend/components/kanshan-app.tsx      ← enterApp("zhihu") 改为跳转 zhihu-adapter OAuth authorize
```

### 5.3 Harness 要点（v0.6 实际落地）

1. **凭证单一来源**：所有服务只通过 `kanshan_shared.load_config()` 读凭证，不再各自写 `os.getenv`。loader 优先级：env > `services/config.yaml` > dataclass 默认。兼容用户现有 flat 结构（`zhihu.ZHIHU_APP_KEY`）与推荐嵌套结构（`zhihu.community.app_key`）。
2. **依赖零新增**：共享 yaml 用 stdlib 手写 parser（支持嵌套、list、comment、quoted string 就够了），避免引入 PyYAML。如果未来 schema 变复杂，parser 可整体替换为 PyYAML 而保持 `load_config()` 签名不变。
3. **日志两路径输出**：`JsonLinesHandler` 写机器可读 jsonl（`output/logs/<svc>-<date>.jsonl`），`ConsoleFormatter` 写人读一行；`configure_logging` 是幂等的，通过 `root._kanshan_configured` 守卫防重复。
4. **Provider 错误翻译**：`errors.py` 的 `from_community / from_oauth / from_data_platform` 把三类响应的 `status / code / Code` 归一成 `ZhihuApiError` 子类，main.py 用 `_handle()` 统一映射 HTTP 状态码。业务服务通过网关看到的始终是 `{request_id, error: {code, message, detail}}` envelope。
5. **OAuth 不落盘**：callback 只渲染 HTML 提示用户把 access_token 拷到 config.yaml。v0.6 不做 token 持久化，保持最小依赖；v0.7+ 可以把 token 存入 profile-service 或 Redis 实现自动续期。

### 5.4 验收证据（v0.6）

- 仓库全量测试：**80 ok / 0 fail**（59 → +13 zhihu-adapter +2 routing +4 multi_persona +9 shared-python）。
- `services/config.yaml` 由 `.gitignore` 保护且 `git check-ignore -v` 验证通过。
- `provider_mode=live` 配合真实 `data_platform.access_secret` 可调通 `/zhihu/hot-list` / `/zhihu/zhihu-search` / `/zhihu/global-search` / `/zhihu/direct-answer`。
- `provider_mode=live` 配合 Community 凭证 + 白名单 `ring_id` 可调通圈子详情、评论列表、故事列表/详情、发布想法/评论/点赞。
- 完成一次 OAuth 授权回跳后，粘贴 `access_token` 进 config.yaml、重启 adapter，即可调通关注列表 / 粉丝列表 / 关注动态。
- 各服务首次请求后在 `output/logs/<service>-<date>.jsonl` 至少留下一条 JSON 日志行，控制台 stderr 也能看到对应人读行。

---

## 6. v0.7 实施切片（预告）

### 6.1 服务边界（v0.7）

- content-service：Query Plan + 多来源聚合 + cardScore + 多样性去重 + serendipity；调 zhihu-adapter 真实数据 + llm-service 真实摘要/争议/角度。
- seed-service：answer-seed-question / supplement-material 调 llm-service 真实任务，替换内部 mock 文本。
- frontend：从 `/api/mock/*` 切到 `:8000/api/v1/*`。

### 6.2 改动范围

```text
services/content-service/app/
├── query_plan.py            ← 新增
├── recall.py                ← 新增（调 zhihu-adapter）
├── aggregate.py             ← 新增（多来源聚合）
├── score.py                 ← 新增（cardScore + 多样性）
└── service.py               ← 接入新模块

services/seed-service/app/
├── llm_client.py            ← 新增（调 llm-service）
└── service.py               ← agent_supplement / answer_question 改走真实 task

frontend/lib/api-client.ts   ← 替换 /api/mock/* → /api/v1/*
frontend/components/kanshan-app.tsx  ← 路径替换
```

### 6.3 验收（v0.7）

详见路线图 v0.7。

---

## 7. v0.8 实施切片（预告）

### 7.1 服务边界（v0.8）

- sprout-service：activationScore 真实计算 + 新信息召回；调 llm-service sprout-opportunities + zhihu-adapter 缓存数据。
- writing-service：argument-blueprint / draft 调 llm-service；roundtable-review 走 v0.5 的 multi_persona 模式得到 4 个 reviewer。
- profile-service：不变（Memory injection 已就绪）。

### 7.2 改动范围

```text
services/sprout-service/app/
├── activation.py            ← 新增（activationScore 算子）
├── recall.py                ← 新增（拉热榜/搜索缓存）
└── service.py               ← 替换 mock initial_opportunities

services/writing-service/app/
├── llm_client.py            ← 新增
├── blueprint.py             ← 新增（替换 _build_blueprint mock）
├── draft.py                 ← 新增
├── roundtable.py            ← 新增（聚合 multi_persona 结果）
└── service.py               ← 接入
```

### 7.3 验收（v0.8）

详见路线图 v0.8。

---

## 8. v0.9 实施切片（预告）

### 8.1 服务边界（v0.9）

- feedback-service：feedback-summary 调 llm-service；二次种子 / Memory 更新建议保持 pending 不写入。
- profile-service：apply 路径前端串通。
- 全局：trace 离线分析、docker-compose 9 服务、Demo 数据预置、AI 辅助声明。

### 8.2 改动范围

```text
services/feedback-service/app/
├── llm_client.py            ← 新增
└── service.py               ← comments_summary / second_seed / memory_update_request 接入

infra/
└── docker-compose.yml       ← 加 9 个服务 service 块

scripts/
└── analyze_llm_trace.py     ← 新增：按 task/provider 聚合

docs/
├── 看山小苗圃-Demo 演示脚本.md  ← 新增
└── P0-mock-闭环-测试checklist.md  ← 升级为 v0.9 完整 checklist
```

### 8.3 验收（v0.9）

详见路线图 v0.9。

---

## 9. 服务边界总表（最终态，v0.9 完成）

| 服务 | 负责 | 不负责 |
| --- | --- | --- |
| api-gateway | request_id、demo user、路由代理、错误归一化 | 业务计算、直连知乎、直连 LLM |
| profile-service | 用户画像、全局/兴趣 Memory、注入摘要、update-requests apply/reject | 内容推荐、种子成熟度、写作状态 |
| zhihu-adapter | 三类知乎 Client、Redis 缓存、quota、字段归一化、mock/live 模式 | 卡片评分、种子、写作、Memory |
| llm-service | task 路由、provider 注册表、persona 编排、prompts、cache、validators、trace、fallback | 业务主状态、直连知乎 API |
| content-service | Query Plan、候选召回、卡片聚合、cardScore、多样性、刷新、来源展示 | 创建种子、发芽、写作、Memory 主存储 |
| seed-service | 种子状态、四格材料、疑问线程多轮、成熟度、合并 | 搜索、推荐、写作、Memory 主存储 |
| sprout-service | 主动触发 run、候选筛选、activationScore、机会生成、补料/换角度/写作动作 | 生成完整文章、Memory 主存储 |
| writing-service | session、memoryOverride 隔离、blueprint、draft、roundtable 聚合、定稿、模拟发布 | 直接覆盖 profile Memory、绕过用户确认发布 |
| feedback-service | 反馈列表、评论摘要、二次种子 payload、memory_update_request 仅 pending | 直接写 Memory、直接发文章 |
| frontend | 状态管理、UI 渲染、按 gateway 调用 | 直连 zhihu-adapter / llm-service、计算成熟度/发芽分 |

---

## 10. 评测指标采集（v0.9 完整化，v0.5 起开始落地 trace）

| 维度 | 指标 | 数据源 |
| --- | --- | --- |
| LLM 成本 | 调用次数、缓存命中率、fallback 率、平均 latency | `output/llm-trace/*.jsonl` |
| 知乎成本 | quota 消耗、缓存命中率 | zhihu-adapter Redis |
| 内容质量 | 卡片来源覆盖率、多来源率、争议有效率 | content-service runtime stat |
| 种子质量 | 重复率、四格覆盖、成熟度提升 | seed-service runtime stat |
| 发芽质量 | activationScore 分布、写作转化率 | sprout-service runtime stat |
| 写作质量 | Memory 可见率、审稿采纳率 | writing-service runtime stat |

v0.5 先落 LLM trace；v0.6 落 quota；v0.7-0.9 各自补本服务 stat。

---

## 11. 风险与对策

| 风险 | 对策 |
| --- | --- |
| 多 provider 让本地启动复杂 | 默认 `LLM_PROVIDER_MODE=mock`；OpenAI 兼容 env 未配则 registry 不注册 |
| 多 persona 让 roundtable 慢 | persona 并发执行；任一失败 mock 替补不阻塞整体；缓存命中跳过所有 persona |
| 业务服务被 llm-service 改动牵连 | v0.5 不动业务服务；契约（task_type + envelope）保持不变 |
| 文档和代码漂移 | 每次 commit 必须同步本文件、路线图、服务 README，CI 后续可加 lint |
| trace 文件膨胀 | 按日期切片；v0.9 加清理脚本（保留 7 天） |
